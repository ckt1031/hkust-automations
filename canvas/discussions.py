# from canvas.api import get_courses, get_discussion_topics


def check_canvas_discussions():
    return

    # courses = get_courses()

    # for course in courses:
    #     discussion_topics = get_discussion_topics(course["id"])

    #     for topic in discussion_topics:
    #         if topic["is_announcement"] is True:
    #             continue

    #         # print(topic["title"])
